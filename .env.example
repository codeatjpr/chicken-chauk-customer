# Backend origin only (no /api/v1). The client appends /api/v1 automatically.
# Example: http://localhost:5000
VITE_API_URL=http://localhost:5000

# Razorpay: publishable key_id is returned by POST /api/v1/payments/initiate — no VITE_ keys required.
